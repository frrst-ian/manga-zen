<nav>
  <ul>
    <% for(let i = 0; i < links.length; i++) { %>
    <li>
      <a class="header-text" href="<%= links[i].href %>">
        <span> <%= links[i].text %></span>
      </a>
    </li>
    <% } %>
  </ul>
</nav>